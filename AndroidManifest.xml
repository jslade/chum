<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="chum.examples"
      android:versionCode="1"
      android:versionName="0.0.1">
    <application android:label="@string/app_name">
        <activity android:name=".ChumExamples"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".FastestPossibleFPS"
            android:launchMode="singleTop"
            android:label="Fastest-possible FPS">
        </activity>                    

        <activity android:name=".BackgroundColorTouch"
            android:launchMode="singleTop"
            android:label="Background color touch">
        </activity>                    

        <activity android:name=".ForceCloseExample"
            android:launchMode="singleTop"
            android:label="Force-close test">
        </activity>                    

        <activity android:name="chum.util.CrashReportMailerActivity" />


    </application>
</manifest> 
